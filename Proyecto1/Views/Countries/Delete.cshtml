@model Proyecto1.Models.Countries

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Country</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.COUNTRY_NAME)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.COUNTRY_NAME)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => Model.Regions.REGION_NAME)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => Model.Regions.REGION_NAME)
        </dd>
    </dl>

    <form id="deleteForm" asp-action="Deletes" asp-route-id="@Model.COUNTRY_ID" method="post">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index" class="btn btn-primary">Back to List</a>
    </form>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

