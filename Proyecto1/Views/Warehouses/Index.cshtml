@model IEnumerable<Proyecto1.Models.Warehouses>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div class="wrapper">
    <p>
        <a asp-action="Create" class="btn btn-success">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.WAREHOUSES_NAME)
                </th>
                <th>
                   LOCATION
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.WAREHOUSES_NAME)
                    </td>
                    <td>
                        @Html.DisplayNameFor(model => Model.Locations.LOCATION_NAME)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.WAREHOUSE_ID" class="btn btn-warning">Edit</a> |
                        <a asp-action="Delete" asp-route-id="@item.WAREHOUSE_ID" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
